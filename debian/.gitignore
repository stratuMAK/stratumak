stratumak
files
rules
compat
control
tmp
stratumak-dev
stratumak-doc-*/
*.substvars

# generated from `.in` by debian/configure
stratumak.install
stratumak.manpages

*.debhelper
debhelper-build-stamp
stratumak.lintian-overrides

# dh_autoreconf build state: md5 manifests of the tree before/after
# autoreconf, consumed by dh_autoreconf_clean. Regenerated by every build.
autoreconf.before
autoreconf.after
