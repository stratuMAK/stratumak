# The Go and npm caches debian/rules points GOMODCACHE and npm_config_cache at
# (see the comments there). They are build output, not source, and are
# read-only by design, so they must not land in the diff of a source package.
extend-diff-ignore = "^\.(gomodcache|npmcache)/"
