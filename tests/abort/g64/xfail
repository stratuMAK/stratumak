Ported to gomc (config: LIB:linuxcnc.hal; motion-test.hal load sampler + single-instance
pins; test.sh: resident gomc-server + halsampler capture; test-ui.py: gmi driver;
sample analysis is mm-aware — joint samples are millimetres, expectations inch).

The original xfail reason (modal G-code state not restored after abort) is FIXED:
milltask now captures the interp's packed state tag per motion segment and runs
Interp::restore_from_tag on abort (2.9 emcTaskStateRestore parity) — all
mode/P/Q checks pass.

Still xfail on a SEPARATE canon gap (PRODUCTION_READINESS.md "G64 blending
extents"): the motion-extent checks for the G64 cycles fail because gomc's
canon does not implement 2.9's naive CAM detector (emccanon.cc chord_deviation
segment merging for G64 Qx — expected max_x 0.0) and the G64/G64 P0.5
corner-rounding extents diverge (max_x stays at the exact-stop 5.0). The G61 /
G61.1 cycles pass. Remove this xfail once canon-side G64 blending parity is
implemented.
