{
  "name": "gomc-webapps-lint",
  "private": true,
  "version": "0.0.0",
  "description": "Shared ESLint toolchain + config for the gomc webapps (lint tooling only; each app builds independently). Versions pinned exact for a deterministic gate without a committed lockfile.",
  "type": "module",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "eslint": "9.39.5",
    "eslint-plugin-vue": "9.33.0",
    "typescript-eslint": "8.65.0",
    "vue-eslint-parser": "9.4.3"
  }
}
