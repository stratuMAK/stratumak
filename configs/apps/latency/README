Latency evaluation programs

latency-test -- Standard test for linuxcnc.  It runs the realtime threads and
opens a web-based viewer with a live summary, latency plot and jitter histogram
for each thread.

Note: latency-test can be run from a terminal command line with more options
(including a --stress mode), for help use:

latency-test --help
